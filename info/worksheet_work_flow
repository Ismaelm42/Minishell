Tasks

  - Señales ( interactivas / no interactivas)

  - Crear copia de environment para trabajar con ella

  - Funciones builds-in:

                ◦ echo con la opción -n.
                ◦ cd solo con una ruta relativa o absoluta. o pwd sin opciones.
                ◦ export sin opciones.
                ◦ unset sin opciones.
                ◦ env sin opciones o argumentos.
                ◦ exit sin opciones.

  -struct tokens:

                char  *infile;  infile.txt / NULL  ( deberia ser un fd o lo crea a partir del string)
                char  *command;
                char  **arg;    // Argumentos Si es Null no existen argumentos
                char  *outfile; outfile.txt o NULL si no existe
                int    fd; // si existe outfile fd seria el outfile

  - << -->  bash-3.2$ << primerdel << segundodel <<tercerdel cat

            > sdfg
            > sdfgh  
            > primerdel
            > dfgh
            > sdfg
            > dfg
            > segundodel
            > hola
            > tercerdel
            hola
- >> hace un append // escribe al final del archivo

NOTA SUBJECT ◦ “<<” debe recibir un delimitador, después leer del input de la fuente actual hasta que una línea que contenga solo el delimitador aparezca. Sin embargo, no necesita actualizar el historial.
◦ “>>” debe redirigir el output en modo append.

- Gestionar $?, que deberá expandirse al estado de salida del comando más reciente ejecutado en la pipeline

